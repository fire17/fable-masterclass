# Changelog

## v1.0.1 — 2026-07-06

The star-magnet printing (docs only; book and skill untouched).

- README: the overhaul that became the /awesome-readme exemplar — hero banner
  ("the circle IS the craft"), 8 live badges, the self-distillation story with
  receipts, two mermaid diagrams, the 16 Laws as a linked table, the full
  making-of with the honest tool inventory and defects-caught list, star CTA.
- The VS16 emoji-anchor lesson (⚖️ keeps an invisible U+FE0F in its rendered id)
  was caught here first and encoded into /awesome-readme as its founding war story.

## v1.0.0 — 2026-07-06

First release.

- **The book** — *THE FABLE MASTERCLASS* (9,572 words): 16 chapters across five parts, each principle → failure-it-prevents → practice → mastery check; the 16 Laws; six pressure checklists; the closing charge to the disciple.
- **The skill** — `fable_masterclass` (alias `/masterclass`): the full doctrine as one-line reminders with 21 machine-verified `[Ch.N L<line>]` references and a smart index.
- **Tooling** — promptless dual-mode backup-first installer; `verify_refs.sh`; CI (ubuntu + macos) proving references + a scratch end-to-end install on every push.
